# About

This project contains a Java application that sends messages to an Apache Kafka topic using the Kafka Producer API.

# Messages
The messages being sent are instances of the `CarPurchase` class, which represents a car purchase event with details such as purchase ID, dealership name, car model, price, and purchase date.